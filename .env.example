# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Polymarket Trading Configuration
# Your private key for signing transactions on Polymarket (Polygon)
# IMPORTANT: Keep this secure! Never share or commit this to git
# NOTE: For multi-user mode, leave this blank and users will create wallets via /create_wallet
PRIVATE_KEY=your_private_key_here

# Polymarket Proxy Address
# Your proxy/funder address on Polymarket (if using email/magic wallet, this is your proxy address)
# If using a direct EOA wallet, set this to the same as your wallet address
POLYMARKET_PROXY_ADDRESS=your_proxy_address_here

# Polymarket Signature Type
# 0 = EOA wallet (direct private key, e.g., MetaMask export)
# 1 = Email/Magic wallet proxy (default)
# 2 = Browser wallet proxy
POLYMARKET_SIGNATURE_TYPE=1

# PostgreSQL Database (for multi-user persistent storage)
# If using Supabase: Get the connection string from Project Settings > Database > Connection String (URI)
# Format: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
DATABASE_URL=postgresql://user:password@host:5432/database

# Google Cloud KMS (Required for encryption)
# See docs/google-cloud-kms-setup.md for setup instructions
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GCP_KMS_KEY_RING=polymarket-bot
GCP_KMS_KEY_NAME=private-key-encryption
GCP_KMS_LOCATION=global

# For local development: use file path
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# For Railway/cloud deployment: paste entire JSON content (recommended for cloud platforms)
# Get this from your GCP service account key JSON file
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# deBridge DLN API Configuration
# Used for same-chain swaps (USDT -> BNB on BSC) and cross-chain bridging
# No API key required - fully decentralized
# Documentation: https://docs.debridge.com/

# Alchemy Webhook Configuration (Optional - for deposit/withdrawal notifications)
# Enables real-time notifications when users deposit/withdraw BNB or USDT
# Sign up at https://www.alchemy.com/ and create a BNB Chain app
ALCHEMY_API_KEY=your_alchemy_api_key_here
ALCHEMY_WEBHOOK_URL=https://your-app.railway.app/webhook/alchemy
WEBHOOK_PORT=8080
